import InProgress from "../element/special-page/in-progress";

const ConjugationScreen = () => {
    return (
        <div className="page-container">
            <InProgress/>
        </div>
    );
}

export default ConjugationScreen;